# Modifications suites erreurs compilation

Correction et respect de la casse dans les #include.
Renommage de synVoix.* en SynVoix.*

SynCalcul.h:
Redéfinition des constantes en constantes de compilation:
#define VOLUME_REF 10	//devrait être 15, mais on le pousse un peu
#define HAUTEUR_REF 6
#define DEBIT_REF 3

SynGlobal.cpp:
Ajout de #include <string.h> car pb strlen()

SynParle.cpp:
Ajout de #include <string.h> car pb strcmp() et strcpy()
Remplacement des sizeof(long) en 4.

SynVoix.cpp:
l87: char* getPtTabDeb(unsigned char c);au lieu de char* Tab::getPtTabDeb(unsigned char c);

SynParle.cpp:
Redéfinition des constantes en constantes de compilation:
#define FREQ_ECH 10000
#define TYPE_ECH 16
#define MUL_AMP 8	//pour max moitié pleine échelle

Synthe.cpp:
Ajout de #include <string.h> car pb strlen()
Ajout d'un #ifdef WIN32 avant la fonction DllMain()

SynTrans.cpp:
Ajout des lignes:
					  // Fonction itoa() non définie en C++ sauf par certains compilateurs
						#ifdef WIN32
						itoa(nbFois, chaine,10);
						#else
						sprintf(chaine, "%d", nbFois);
						#endif

alsa.cpp:
Ajout de #include <iostream>
Ajout de using namespace std;
